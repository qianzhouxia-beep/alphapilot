// AlphaPilot US Market Dashboard — 复用 / 的 server logic, HeaderBar market=us
// M2 2026-06-06: Boss pick 双市场独立路由

import Dashboard from "@/app/page";

export default function USDashboard() {
  return <Dashboard />;
}
