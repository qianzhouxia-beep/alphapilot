# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0  # for EmailStr in auth schemas

# Database (async)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0  # W2: enable migrations

# Cache
redis==5.2.1

# HTTP client (Polygon, etc.)
httpx==0.28.1
websockets==14.1  # W2: Polygon WebSocket

# Auth (W2)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Payments (W2)
stripe==11.4.1

# Logging
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0

# Data processing (used by Ashare.py and screener services)
pandas==2.2.3
numpy==2.1.3

# force-rebuild 2026-06-07 00:25 (clear Zeabur cache for pandas+numpy)
